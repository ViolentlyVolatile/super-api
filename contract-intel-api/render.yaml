# NOTE: the active Render Blueprint lives at the repo ROOT (../render.yaml),
# because Render only reads render.yaml from the repository root.
# This file is kept as a pointer so the service folder is self-documenting.
